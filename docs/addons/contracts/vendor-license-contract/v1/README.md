# Vendor License Contract V1 — compatibility pointer

Faza 03 je implementirala HTTP namespace `/api/v1`, payload `contractVersion: 1` i `NRLS-HMAC-V2`. Stvarni deterministički JSON fixture-i nalaze se u [`../../vendor-license/v1/`](../../vendor-license/v1/); ova ranije rezervisana putanja se ne sme predstavljati kao drugi source of truth.

Signed entitlement/release manifest compatibility vektori još nisu kompletni i ostaju acceptance gap iz finalne verifikacije.
